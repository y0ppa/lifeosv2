/* Copy this file to supabase-config.js and fill in your project's values.
   Both are safe to expose in client-side code by Supabase's own design —
   the anon key only ever grants what Row Level Security policies allow.
   Find these under Project Settings -> API in the Supabase dashboard. */
window.BRAIN_SUPABASE_URL = 'https://YOUR-PROJECT-REF.supabase.co';
window.BRAIN_SUPABASE_ANON_KEY = 'YOUR-ANON-PUBLIC-KEY';
